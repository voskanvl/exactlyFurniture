// const select = document.querySelector('.select > select');
// const selectTitle = select.querySelector('optgroup');

// select.addEventListener('change', (event) => {
//   selectTitle.label = event.target.value;
// });
